# gitlocal Development Guidelines

Auto-generated from all feature plans. Last updated: 2026-09-20

## Active Technologies
- **Runtime**: Node.js 22+ (active LTS), TypeScript 5.x
- **HTTP server**: Hono ^4.x + @hono/node-server ^1.x
- **Frontend**: React 18, Vite 7, @tanstack/react-query, react-markdown, remark-gfm, rehype-highlight
- **Build**: esbuild (server bundle), Vite 7 (UI bundle)
- **Test**: Vitest + @vitest/coverage-v8 (≥90% per-file branch coverage enforced)
- **State**: All state in-process or derived from filesystem/git at request time
- **Terminal (032)**: `node-pty` (server PTY sessions), `ws` (per-tab WebSocket I/O), `@xterm/xterm` + `@xterm/addon-fit` (UI rendering) — see `specs/032-integrated-terminal-panel/research.md`
- **File preview (036)**: `pdfjs-dist` (PDF rendering, lazy-loaded, worker bundled locally via Vite `?url` import — never a CDN URL) — see `specs/036-file-preview-framework/research.md`
- **CSV/Excel preview (038)**: `papaparse` (CSV parsing, MIT), `xlsx` (SheetJS Community Edition, Apache-2.0, pinned to the SheetJS-published CDN tarball rather than the stale npm registry copy) — both lazy-loaded — see `specs/038-csv-excel-viewer/research.md`
- **PPTX preview (039)**: `jszip` (MIT, unzips the OOXML container) + `fast-xml-parser` (MIT, parses slide/notes XML) — both lazy-loaded, no pixel-faithful rendering library used (none is free/license-compatible) — see `specs/039-bugfix-pptx-preview/research.md`

## Project Structure

```text
src/          # TypeScript server source
tests/        # Vitest unit and integration tests
ui/           # React frontend (Vite)
dist/         # Compiled server bundle (gitignored)
ui/dist/      # Compiled UI bundle (gitignored)
```

## Commands

```sh
npm test      # Run all tests with coverage
npm run lint  # tsc --noEmit type check
npm run build # Build server + UI bundles
```

## Product Direction

GitLocal is for less-technical builders working in an AI-driven development lifecycle. The product should optimize for codebase browsing, Markdown reading, review, and lightweight human intervention rather than full-IDE workflows.

GitLocal has two distributions using the same app code:
- npm package: cross-platform, one-command install, browser-based, requires the terminal process to stay open.
- macOS Homebrew cask: native `GitLocal.app`, embedded WebKit browser, managed local service lifecycle.

Measured on the current branch, 90.7% of implementation lines are shared between both distributions, excluding tests, docs, and generated build output.

## Code Style

TypeScript 5.x + Node.js 22+: follow standard conventions. Use `.js` extensions on all imports (NodeNext module resolution). Keep product server, CLI, and UI behavior in the existing TypeScript/React stack. Swift is permitted only for the scoped macOS native wrapper under `native/macos/`, and shell/Ruby packaging files are permitted only for Homebrew/macOS release automation under `packaging/macos/` and `.github/workflows/`.

## Recent Changes
- 046-website-navigation (site only, no npm release): Split the single-page gitlocal.dev (`docs/`) into real URLs so per-topic interest and topic-to-topic paths are measurable: `/`, `/features/` (hub) plus eight topic pages `/features/{browse,search,markdown,previews,review,git-state,edit,terminal}/`, `/install/`, `/community/`, `/faq/`, and a real `docs/404.html`. Gotchas: the site deploys to Cloudflare Pages via `.github/workflows/deploy-website.yml` (no build step; `docs/` is served as-is, and the workflow injects `/vizoalica-loader.js` at deploy time), so **every page must carry `<script async src="/vizoalica-loader.js"></script>`** or navigation through it is invisible to analytics; Pages serves `index.html` with a 200 for any unknown URL unless a `404.html` exists, which is why one was added; all links are page-relative (not root-absolute) so the `ehud-am.github.io/gitlocal/` mirror keeps working, except `404.html`, which uses absolute `https://gitlocal.dev/` URLs because it is served at arbitrary depths. Home keeps `#features`/`#install`/`#community`; the old `#faq` anchor redirects to `/faq/` (`script.js`); `FAQPage` JSON-LD moved to `/faq/`. Pages are hand-maintained static HTML (no generator is committed), so `tests/integration/website-structure.test.ts` enforces the invariants: analytics tag on every page, unique titles/descriptions, one h1, self-canonical, sitemap parity, all relative links and `#fragments` resolving, identical main nav, hub reaches every topic, valid JSON-LD.
- 045-community-participation (0.13.6): Documentation/config-only release inviting community participation via GitHub. Enabled GitHub Discussions and private vulnerability reporting (repo settings, not versioned), added `.github/ISSUE_TEMPLATE/` forms (`bug_report.yml`, `improvement.yml`, `config.yml` with blank issues disabled and ideas/questions routed to Discussions), a PR template, and `SECURITY.md`; added "Ways to Take Part" to `CONTRIBUTING.md`, a "Help Shape GitLocal" section to `README.md`, and a "Built in the open" section + FAQ entry (mirrored in JSON-LD and `docs/llms.txt`) to gitlocal.dev. Key gotcha: the **npm listing is `packaging/npm/README.md`, not `README.md`** (swapped in by the `prepack` hook), so community copy must be edited in both, with absolute URLs in the npm one; a test in `tests/integration/npm-package-contents.test.ts` now guards it. Also corrected stale bottom-only/Claude-Codex terminal wording in the README, site, and `llms.txt`. No application source changed. See `releases/0.13.6-release-review.md`.
- 044-simplify-terminal-panel (0.13.5): Removed the "Claude"/"Codex" terminal-tab kind concept — every terminal tab is now a plain shell (users type `claude`/`codex` themselves if they want them), deleting `TerminalKind`, `TerminalCapabilities.claudeCliFound`/`.codexCliFound`, the `cli_not_found` pre-flight, the server-side auto-launch write, `src/terminal/cli-detection.ts` (its unrelated `isPtySupported` check moved to a new `src/terminal/pty-support.ts`), `ui/src/components/TerminalPanel/TerminalKindSelect.tsx`, and the now-unused `/api/terminal/capabilities` route/handler/client method. Added a configurable terminal dock position (bottom/left/right, default right) persisted server-side to `~/.gitlocal/terminal-panel-preference.json` via new `GET`/`PUT /api/terminal-panel-preference` routes (`src/services/terminal-panel-preference.ts`, mirroring `startup-preferences.ts`'s pattern); `TerminalPanel.tsx` is now orientation-aware (height/row-resize for bottom, width/col-resize for left/right) and `App.tsx` wraps the app-body region and the panel in a single always-present flex container whose direction and `order-first` class toggle with position, so the panel is never remounted by a position change and open tabs/sessions/scrollback always survive it. Unified the "open a new terminal" control into one consistently labeled `NewTerminalButton` used identically in the empty state and the tab strip, and fixed a real labeling bug found during implementation where closing a tab and opening a new one could reuse an already-used tab number (now a monotonic per-panel counter). A pre-release contrarian QA review found and fixed 3 more issues before the `v0.13.5` cut (a non-hermetic test relying on the real `~/.gitlocal` preference file being absent, a temp-directory test-cleanup leak that also surfaced a latent env-var-restore bug, and an accessible-name/visible-text mismatch on the new-terminal button's busy state) — see `releases/0.13.5-release-review.md`. A follow-up UI-design round (still pre-release) replaced `DockPositionControl`'s dropdown with three VS Code-style icon buttons, made `NewTerminalButton` icon-only and moved it (plus the dock-position/collapse buttons) out of `TerminalTabStrip` into `TerminalPanel`'s right-side toolbar (tabs left, all actions right, one "collapse" ✕ button replacing the old Show/Hide toggle), and fixed a real bug where the terminal failed to render at all when docked left/right — per-tab containers now use `position:absolute; inset:0` inside a `position:relative` wrapper instead of nested flex-percentage sizing, and `TerminalView`'s xterm `FitAddon` does one extra `fit()` on the next animation frame as a defensive guard against a 0×0 first measurement. See `specs/044-simplify-terminal-panel/tasks.md`'s "Post-Implementation Follow-Up" section and `specs/044-simplify-terminal-panel/`. A third feedback round fixed a collapsed left/right-docked panel showing an oddly-placed toolbar instead of fully hiding (added a `showChrome` flag so a collapsed side dock now renders nothing, width `0px`, matching bottom dock's existing hidden-when-collapsed look), changed the default side-dock width from a fixed 420px to a third of the window (`window.innerWidth / 3`), and added an `effectiveDockPosition` to `useTerminalPanelPreference` that auto-reroutes to bottom on a window narrower than 700px — reversibly, without touching the stored preference — plus hover tooltips on the remaining untitled toolbar controls. A fourth round fixed the same presentation bug for the *empty* (no tabs yet) state, not just the collapsed one: a side-docked panel's invitation bar previously rendered unconditionally regardless of `panel.state.visible`, so it appeared immediately on first app load whenever the saved position was left/right; it now stays hidden (same zero-width marker) until a tab exists or a create attempt fails (which still shows the error, regardless of dock position).
- 043-footer-pptx-fixes: Restyled the web app footer's GitHub/gitlocal.dev links with paired icons (`GitHubIcon`/`GlobeIcon` in `ui/src/components/ui/icons.tsx`) in a muted developer-tool pattern, and fixed those links not opening at all in the native macOS app — its `WKWebView` had no navigation-policy handler for external/new-window navigation, so `target="_blank"` clicks silently did nothing there; added `webView(_:decidePolicyFor:decisionHandler:)` in `ViewerWindowController.swift` routing such navigation to `NSWorkspace.shared.open`. Also substantially reworked the PPTX preview (`PptxViewer.tsx`): (1) resolves placeholder geometry, background fill, and run/background colors through the OOXML slide→layout→master inheritance chain, including theme-scheme color references (`a:schemeClr`, resolved via the master's `<p:clrMap>` and its `<a:theme>` part's `<a:clrScheme>` — the form real-world decks actually use for backgrounds, not literal RGB) and the `a:sysClr`/`lastClr` color form; (2) renders non-placeholder decorative content (logos, background graphics, static captions) defined directly on a slide's layout/master, composited master-under-layout-under-slide to match PowerPoint's own rendering, with hidden (`hidden="1"`) shapes skipped and EMF/WMF images (no browser-native decoder) shown as "not available" rather than a broken image; (3) replaced Previous/Next button pagination with continuous scroll, an `IntersectionObserver`-driven "Slide N of M" position indicator (scoped to the actual scrolling ancestor, not the page viewport — a real bug caught during manual verification), and inline per-slide notes; (4) made the slide canvas responsive via a `ResizeObserver`-measured frame + CSS `transform: scale` instead of a fixed-pixel canvas that could overflow the preview panel. Also fixed a pre-existing build-verification gap: the repository-root `npm run lint` never actually type-checks `ui/` (excluded from the root `tsconfig.json`) — verifying UI changes requires `cd ui && npx tsc --noEmit` (or `npm run build:ui`) too. A pre-release contrarian QA review (8 independent passes over the diff) then found and fixed 5 more issues before the `v0.13.4` cut merged (a keyboard-navigation regression from the scroll rework, a fragile per-slide scroll-root DOM walk, an ambiguous placeholder-matching edge case, hidden placeholders leaking into inheritance, and a native macOS check narrower than its own design doc — see `releases/0.13.4-release-review.md`), plus a third follow-up round closing its 3 deferred findings: `p:bgRef` themed-background resolution (an approximation — see tasks.md for why exact positional fidelity isn't attempted), full recursive `p:grpSp` group-shape flattening with transform composition, and a parsing-deduplication/`Promise.all`-parallelized-image-loading cleanup pass. See `specs/043-footer-pptx-fixes/` (including its `tasks.md` "Post-Implementation Follow-Up" sections documenting all three follow-up rounds).
- 042-footer-about-links-hosting: Added a `gitlocal.dev` link to the web app footer (`AppFooter.tsx`) alongside the existing GitHub project link, and added the same two links to the native macOS app's "About GitLocal" panel by attaching a link-attributed `credits` `NSAttributedString` to AppKit's standard about panel (`AppDelegate.swift`'s new `showAboutPanel(_:)`), rather than building a custom About window. Also migrated `gitlocal.dev`/`www.gitlocal.dev` hosting from GitHub Pages to Cloudflare (GitHub Pages' native `github.io` URL continues to serve the same `docs/` site unchanged as a mirror); per explicit instruction, the step-by-step Cloudflare/DNS runbook was delivered conversation-only and intentionally has no corresponding file in this repository — see `specs/042-footer-about-links-hosting/research.md` and `plan.md`'s Constitution Check (Principle VII exception) for why. Also closed a coverage-gate gap where `AppFooter.tsx` was missing from `ui/vitest.config.ts`'s coverage `include` list (same class of gap as spec 038's CSV/Excel viewers).
- 041-simplify-startup-logic: Simplified startup folder resolution end to end. Collapsed the 5-tier `StartupFolderSource` (`explicit`/`last-used`/`platform-default`/`home-fallback`/`safe-fallback`) into 3 (`explicit`/`last-used`/`os-default`), replacing `resolveGuaranteedFallbackPath`'s 5-candidate chain (Documents/XDG → home → cwd → tmp → filesystem root) with a single `resolveOsDefaultLocation` (`src/services/startup-preferences.ts`) that tries only the home directory, falling through once to the filesystem root — every startup failure (a bad explicit path, a bad/missing remembered folder, a blocked native file-open, a mid-session self-heal, a parent-folder walk with no readable ancestor) now converges on this one location and shape instead of four independently-computed fallbacks. Extracted `resolveDirectOpenTarget` (`src/git/repo.ts`) as the single repo-vs-independent-folder classifier for directly-opened files, replacing duplicate `dirname`/`relative`/`basename` logic previously computed separately in `resolveOpenTarget` (`src/server.ts`) and `repositoryOpenHandler` (`src/handlers/repo.ts`). Added a defense-in-depth guard to `writeStartupFolderPreference` rejecting any path inside a repository that isn't that repository's root, and removed the unused, unvalidated `PUT /api/startup-folder` endpoint (confirmed zero UI call sites) that was the one surface able to bypass that guarantee. See `specs/041-simplify-startup-logic/`.
- 040-pdf-quality-about-menu (0.13.1): Fixed PDF preview looking fuzzy on Retina/HiDPI displays by scaling the PDF.js render viewport and canvas backing store by `window.devicePixelRatio` (falling back to 1 if unavailable) while pinning the canvas's CSS size back to the original logical scale, so on-page layout and zoom behavior are unchanged (`PdfViewer.tsx`). Added an "About GitLocal" item to the native macOS app's menu (first item in the app menu, above "Set as Default Markdown Reader"), wired to AppKit's built-in `orderFrontStandardAboutPanel(_:)` so it shows the app icon and version with no custom UI code (`AppDelegate.swift`). See `specs/040-pdf-quality-about-menu/` and `releases/0.13.1-release-review.md`.
- 0.13.0 (standalone fix, same release as 039): Fixed a long-standing "empty folder view on launch" problem. `initializePaths` (`src/server.ts`) previously committed to a repo path that had never been checked for existence or readability, so a deleted/renamed/no-longer-readable "last used" or explicit-launch folder silently rendered as a normal, empty, non-git folder with no explanation — matching an existing test (`tests/integration/server.test.ts`) that had locked in this behavior as intentional. Added `resolveGuaranteedFallbackPath` (`src/services/startup-preferences.ts`), a cross-platform chain (Documents → home → cwd → OS temp dir → filesystem/drive root) that always resolves to a verified-readable directory; wired it into `initializePaths`, a new self-healing check at the top of `infoHandler` (recovers mid-session, e.g. an external drive unmounted while the app was already open, on the next `/api/info` fetch), and `repositoryParentFolderHandler` (walks past an unreadable ancestor instead of landing on it, fixing the "go up a folder" workaround so it can't hit the same dead end one level up). A new `'safe-fallback'` `StartupFolderSource` surfaces a clear on-screen message via the existing picker-page banner mechanism in all these cases.
- 039-bugfix-pptx-preview: Four security/correctness/reliability bug fixes (repo symlink write-escape containment re-check on every intermediate path segment and immediately before `mkdir`/`writeFile`; branch-search colon-in-filename path parsing fixed to a length-based `${branch}:` prefix strip instead of colon-splitting; changed-file review now reads `git status` via `--porcelain=v1 -z` (NUL-delimited records) instead of the newline-delimited default, sidestepping Git's C-quoting of renamed paths entirely rather than decoding it; non-terminal WebSocket upgrade attempts close promptly) plus a new read-only PPTX preview capability (`PptxViewer.tsx`, via lazily-loaded `jszip` + `fast-xml-parser`, best-effort formatted slide rendering — positioned text runs, background fill, embedded images — with speaker notes and next/previous navigation, following the same registry-pattern integration as PDF/CSV/Excel with zero `ContentPanel`/registry-dispatch changes, matching spec 036's pattern). Pixel-faithful slide rendering is explicitly out of scope (no free/license-compatible renderer exists), the same tradeoff pattern as spec 038's chart-data-as-table decision. See `specs/039-bugfix-pptx-preview/`.
- 038-csv-excel-viewer: Extended the preview registry with `.csv` (`CsvViewer.tsx`, lazily-loaded `papaparse`, rendered as a scrollable table with raw/pretty toggle) and `.xlsx`/`.xls` (`ExcelViewer.tsx`, lazily-loaded `xlsx`/SheetJS CE, per-sheet tab strip, cached cell values only — never formula recalculation) preview support, both `editable: false`, with zero changes to `ContentPanel.tsx`'s core dispatch logic (SC-006, matching spec 036's pattern). User Story 3 (chart indicators) shipped at reduced scope after empirically confirming SheetJS Community Edition exposes no chart title or cached series data for any chart shape and gives no detectable signal at all for a chart embedded inside a normal worksheet — only a static "this sheet contains a chart" label is shown, and only on a sheet that is itself a dedicated chart tab; confirmed with the feature requester before implementation. Also fixed a pre-existing gap where `ui/vitest.config.ts`'s coverage `include` list omitted the preview-framework's own viewer components. See `specs/038-csv-excel-viewer/`.
- 0.11.0: Linked gitlocal.dev from the npm and GitHub READMEs (website badge, homepage field) and optimized the gitlocal.dev site for AI answer engines and search engines (canonical/OG/Twitter meta tags, JSON-LD `SoftwareApplication`/`FAQPage` structured data, a matching visible FAQ section, `robots.txt`, `sitemap.xml`, `llms.txt`) — see `releases/0.11.0-release-review.md`.
- 036-file-preview-framework: Introduced a registry-pattern preview framework (`ui/src/components/ContentPanel/preview-registry.tsx`) mapping each file content type to its preview component, migrating all existing preview behavior (markdown, json, text, image, binary) onto it with zero behavioral change, and extended it with read-only PDF preview (`PdfViewer.tsx`, via lazily-loaded `pdfjs-dist` with a locally-bundled worker) and read-only SVG preview (`SvgViewer.tsx`, rendered as an inert `<img>` data URI to prevent script execution) — see `specs/036-file-preview-framework/`.
- 035-quality-hardening (0.10.3): Full architecture/code review of all 15 reviewable units (110 findings logged) and disposition of all of them — bug fixes (sync-status path-type misreporting on non-current branches, an opened-file name mismatch outside git repos, macOS app JS-injection and process-lifecycle races, missing file-tree keyboard navigation, incorrect picker `aria-expanded` state, a silent failed-subdirectory-fetch with no retry indicator, "Find in file" losing its query on reopen, stale search pagination across a branch switch, a macOS default-Markdown-reader partial-registration gap), plus dead-code removal, duplicate-code consolidation, and efficiency/readability polish across the server, UI, and macOS app (Phases 5-8). A pre-release contrarian QA pass found and fixed two additional issues: a non-keyboard-accessible folder-picker sidebar (converted to native buttons) and an unfixed effect-dependency inefficiency in the file tree. See `specs/035-quality-hardening/` and `releases/0.10.3-release-review.md`.
- 034-patch-bugfixes: Six patch-level bug fixes/relocations — restored folder-view scrolling (`min-h-0` on `content-area`), fixed the Claude/Codex terminal-tab PATH detection error, added a Ctrl+` terminal-toggle shortcut, unified the dotfile-toggle checkboxes into a single toolbar "View options" dropdown (with a native macOS View-menu counterpart), moved Refresh into that dropdown behind a Ctrl+Alt+R shortcut, and moved the Tracked/All/Local selector into the same dropdown (plus a native submenu) — see `specs/034-patch-bugfixes/`.
- 033-ui-density-navigation-fixes: UI-only density/navigation fixes — two-row repository block, Parent Folder control moved to the top toolbar, toolbar button restyling, denser folder-view spacing with a redundant nested container removed, terminal collapsed-state viewport fix, smaller terminal font — see `specs/033-ui-density-navigation-fixes/`.
- 032-integrated-terminal-panel: Added a persistent bottom-docked terminal panel (Regular/Claude/Codex tab kinds, cross-page session persistence via `node-pty` + per-tab WebSocket I/O, cwd-follows-visible-content) — see `specs/032-integrated-terminal-panel/`.
- 018-macos-homebrew-app: Added scoped macOS native app packaging around the shared GitLocal server/UI while preserving the npm package distribution.

<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->

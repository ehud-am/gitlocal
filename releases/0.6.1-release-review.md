# GitLocal v0.6.1 Release Review

## Scope

- Release: `v0.6.1`
- Feature: main-view folder delete action and compact navigation tags
- Branch: `012-folder-delete-action-tags`
- Release date: 2026-05-02

## Verification Reviewed

- `npm test` passed locally.
- `npm run build` passed locally.
- `npm run verify` passed locally with server tests, UI tests, build, and root/UI audits.
- Root and UI npm audits reported 0 vulnerabilities.

## Contrarian QA Findings

Contrarian QA sub-agent review ran before commit and initially identified release-readiness blockers:

- Package metadata still reported `0.6.0`.
  - Result: Fixed. `package.json` and `package-lock.json` now report `0.6.1`.
- Release notes/status artifacts were missing for `0.6.1`.
  - Result: Fixed. `CHANGELOG.md`, this release review, and the feature spec status now reference the `0.6.1` release.

The QA review found no functional blocker in the moved delete action, left-tree delete removal, root/file exclusion, typed confirmation reuse, or compact tag behavior.

## Accessibility Review

- Existing axe-backed UI tests remain passing.
- The moved delete-folder action keeps explicit button text.
- The existing alert-dialog typed confirmation remains the final destructive gate.
- Compact local tags remain textual rather than icon-only.

## Release Documentation Review

- `CHANGELOG.md` includes a `0.6.1` entry.
- `README.md` was reviewed and updated to describe deleting the current subfolder from the main folder view.
- `specs/012-folder-delete-action-tags/spec.md` is marked ready for the `v0.6.1` release.

## Follow-Up Status

- Release blockers from contrarian QA: addressed.
- Verification: passed locally before commit.
- Publication status: pending GitHub release and npm publish workflow.
